import React from 'react'
import { Segment, Form, TextArea, Input, Icon, Button, Container, Grid } from 'semantic-ui-react'

const StoryForm = ({ storyData, handleSaveDraft, handleChange, handleSubmit }) => {
  return(
    <Container>
      <Segment>
        <Form>
        
        </Form>
      </Segment>
    </Container>
  )
}

export default StoryForm
